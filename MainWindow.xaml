<Window x:Class="Movie_Tracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Movie_Tracker"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#2b2b2b">
            <TextBlock Text="🎬 Movie Tracker" Foreground="White" FontSize="20" FontWeight="Bold" Margin="15,20,15,30" TextAlignment="Center"/>

            <Button x:Name="BtnMovies" Content="{DynamicResource MenuMovies}" Height="40" Margin="10,5" Click="BtnMovies_Click"/>
            <Button x:Name="BtnSettings" Content="{DynamicResource MenuSettings}" Height="40" Margin="10,5" Click="BtnSettings_Click"/>
        </StackPanel>

        <Frame x:Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden" Background="#f0f0f0"/>
    </Grid>
</Window>
